type Person{
    id: ID!
    name: String!
    age: Int!
    nation: String!
}
type Query{
    people: [Person]
    person(id: Int!): Person
}
type Mutation{
    addPerson(name: String!, age: Int!, nation: String!): Person!
}